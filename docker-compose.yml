services:
  algo:
    build: .
    container_name: bybit_0dte_s3
    restart: always
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=UTC
      - ENTRY_NOW=${ENTRY_NOW:-false}
    volumes:
      - ./state:/app/state
      - ./logs:/app/logs
